// This layout will verify auth via requireUser() in future stories (Story 1.3).
// For now, minimal stub — Story 1.3 wires the auth redirect.
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
